/**
 * Rotas do frontend.
 * Não confundir com `src/api/endpoints.ts`, que são as rotas do backend.
 */
export const PATHS = {
  root: "/",
  login: "/login",
  register: "/register",
  boards: "/boards",
} as const
